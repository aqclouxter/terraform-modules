# S3 Module

Este módulo crea un bucket S3 privado en AWS.

## Variables

- `bucket_name` → nombre del bucket
- `region` → región de AWS (default: us-east-1)

## Outputs

- `bucket_name` → nombre del bucket creado

## Ejemplo

Ver `examples/minimal/main.tf`
